<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/index.css">
</head>
<body>
    <div class="container">
        <div class="login">
            <form >
            <h1>Login</h1>
            <hr>
            <p>Login to get more new things</p>
            <label for="username" class="username">Username</label>
            <input type="text" placeholder="username" id="username">
            <div class="photos">
                <p>Choose your profile photo</p>
                <% for(let i = 0; i < 3; ++i) {%> 
                    <div class="checkbox-container <%= i===0? 'now': undefined%>" >
                    <input class="check" type="checkbox" id="image<%= i %>" value=<%= i %> >
                    <label for="image<%= i %>"><img src="/assets/images/<%= i+1 %>.jpg" alt=""></label>
                    </div>
                <% } %> 
            </div>  
        </form>
        <div class="btn-container">
            <button>Submit</button>
        </div>
        </div>
    </div>
    <script src="/socket.io/socket.io.js"></script>
    <script src="./js/index.js"></script>
</body>
</html>